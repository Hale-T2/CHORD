# Runtime dependencies of chordlab and the notebooks (pip install -r requirements.txt, or: pip install -e .)
numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
librosa>=0.10
soundfile>=0.12
mir_eval>=0.7
pretty_midi>=0.2.10
ipython>=8.0
jinja2>=3.0
# optional: extract single tracks from the 4.1 GB RWC-P zip via HTTP range requests
# remotezip>=0.12
